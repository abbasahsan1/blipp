/** Material bottom navigation height, excluding the bottom safe-area inset. */
export const TAB_BAR_HEIGHT = 62;

/** Height of the persistent mini player docked above the tab bar. */
export const MINI_PLAYER_HEIGHT = 62;

/** Horizontal/vertical breathing room around the docked mini player. */
export const MINI_PLAYER_GAP = 8;

/** Space a scrollable screen must reserve so the mini player never covers content. */
export const MINI_PLAYER_INSET = MINI_PLAYER_HEIGHT + MINI_PLAYER_GAP * 2;
